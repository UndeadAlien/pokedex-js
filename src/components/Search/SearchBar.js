import React, {useEffect} from 'react'

const SearchBar = ({ posts }) => {

    useEffect(() => {

    }, []);

    return (
        <div>
            
        </div>
    )
}

export default SearchBar;
